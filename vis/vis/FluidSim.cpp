#include "FluidSim.h"

// Should just call particles.reset()
void FluidSim::reset() {
    all_particles.reset();
}

// Calls step particles.step() with appropriate params
void FluidSim::step() {
    
}